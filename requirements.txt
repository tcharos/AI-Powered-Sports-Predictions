# Scraping
scrapy==2.13.4
scrapy-playwright==0.0.44
beautifulsoup4==4.14.3
requests==2.32.5

# Data Science & ML
pandas==2.3.3
numpy==2.3.5
xgboost==3.1.2
scikit-learn==1.7.2
rapidfuzz==3.14.3
thefuzz==0.22.1

# Web UI
flask==3.1.2

# NBA
# pbpstats dropped: its rich endpoints need stats.nba.com via providers our
# network couldn't reach with raw requests; nba_api ships proper headers and
# works. nba_api covers the daily refresh — fixtures (ScoreboardV3) + appending
# fresh game-log rows (LeagueGameLog) — see ml_project/nba/fetch_nba_daily.py.
# Historical training corpus is built from a local archive — see
# ml_project/nba/process_archive.py.
nba_api==1.11.4

# Real betting (dormant — macOS Keychain credentials + Playwright stealth)
keyring==25.7.0
playwright-stealth==2.0.3

# UI docs renderer
markdown==3.10.2
