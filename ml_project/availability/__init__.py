"""Player-availability adjuster package (D4).

N2 (`sofifa_importance`): join the Flashscore "Will not play" list (N1) to SoFIFA
player ratings → per-absentee *importance* weight. N3 (`adjust`, not yet built)
turns those weights into a capped, post-model probability shift.
"""
